{ config, pkgs, ... }:

{
  ################################################################################
  #
  #  通用模块入口
  #
  #  这里用于导入 modules 目录下的所有自定义模块
  #  可以将 nixos 和 home-manager 的模块都在这里汇总
  #
  ################################################################################

  imports = [
    # 导入 NixOS 模块
    # ./nixos/some-module.nix

    # 导入 Home Manager 模块
    # ./home-manager/some-module.nix
  ];
}
